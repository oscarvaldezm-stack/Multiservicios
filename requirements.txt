fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30,<2.2
psycopg[binary]>=3.2
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
python-multipart>=0.0.9      # requerido por OAuth2PasswordRequestForm
PyJWT>=2.9                   # se prefiere sobre python-jose (sin mantenimiento activo)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                # passlib 1.7.4 es incompatible con bcrypt>=4.1 (warnings) y >=5 (falla)
cryptography>=43            # AES-256-GCM para CURP, RFC y números de documento
Pillow>=10.3                # validación y re-codificación de imágenes (quita EXIF/GPS)
pypdf>=3.17,<6              # inspección estructural de PDF (JavaScript, adjuntos, cifrado)
pypdfium2>=4.20             # PDF -> imagen para el revisor (nunca se le entrega el PDF)
boto3>=1.34                 # S3 con SSE-KMS
stripe>=15.6,<16            # SDK oficial de Stripe; la versión de la API se fija en STRIPE_API_VERSION

# Pruebas
pytest>=8.0
httpx>=0.27
